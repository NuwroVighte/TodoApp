import React from 'react'; //doesn't need { Component } because this is a function component and doesn't need the component class

//Function component example
function ThirdComponent() {
    return (
      <div className="ThirdComponent">
        ThirdComponent
      </div>
    );
  }

  export default ThirdComponent;